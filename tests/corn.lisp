(defpackage corn-test
  (:use :cl
        :corn
        :prove))
(in-package :corn-test)

;; NOTE: To run this test file, execute `(asdf:test-system :corn)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
